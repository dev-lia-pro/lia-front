/**
 * Configuration centralisée pour la pagination dans l'application
 *
 * Modifiez ces valeurs pour ajuster la pagination de toutes les sections
 */

/**
 * Nombre d'éléments par page pour toutes les sections paginées
 * @default 20
 */
export const PAGE_SIZE = 20;

/**
 * Taille de page pour les sections "infinies" (projets, providers, etc.)
 * Ces sections chargent tous les éléments d'un coup
 * @default 10000
 */
export const INFINITE_PAGE_SIZE = 10000;
